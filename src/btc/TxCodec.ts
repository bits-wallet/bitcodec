import { IBitcodec } from "../models/IBitcodec";
import { buffer2hex, hex2buffer } from "./helper";

/* export class TxCodec implements IBitcodec<any> {
  encodeBytes: number;
  decodeBytes: number;
  encodingLength: (t?: any) => number;
  encode: (value: any, buffer?: Buffer | undefined, offset?: number | undefined) => Buffer;
  decode: (buffer: Buffer, offset?: number | undefined, end?: number | undefined) => any;
} */
